
    <h1>Homepage</h1>
    <% @photos.each do |photo| %>
      <div>
        <h3> <a href="/photos/<%= photo[:id]%>"><%= photo[:title] %> </a></h3>
        <img src="<%= photo[:source] %>" alt="<%= photo[:title] %>">
      </div>
    <% end %>
